identity:
  name: table_schema
  author: shuzhiyinhang
  label:
    en_US: Get Table Schema
    zh_Hans: 获取表结构
  description:
    en_US: Retrieve columns, types, indexes and metadata of one or more tables in VexDB.
    zh_Hans: 获取 VexDB 表的字段类型、索引、元信息等。
  icon: icon.svg

type: action
parameters:
  - name: tables
    type: string
    required: false
  - name: schema
    type: string
    required: false
    form: form
  - name: db_uri
    type: string
    required: false
    form: form

extra:
  python:
    source: tools/table_schema.py
