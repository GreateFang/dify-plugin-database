identity:
  name: sql_execute
  author: shuzhiyinhang
  label:
    en_US: SQL Execute
    zh_Hans: 执行 SQL
  description:
    en_US: Execute raw SQL statements in VexDB.
    zh_Hans: 在 VexDB 中执行任意 SQL 语句。
  icon: icon.svg

type: action
parameters:
  - name: query
    type: string
    required: true
  - name: db_uri
    type: string
    required: false
    form: form

extra:
  python:
    source: tools/sql_execute.py
